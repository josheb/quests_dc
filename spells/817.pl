sub EVENT_SPELL_EFFECT_CLIENT
{
   #my $cl = $entity_list->GetClientByID($caster_id);
   #if ($cl)
   #{
   #     $cl->CastSpell(1636, $caster_id);
   #}
}

sub EVENT_SPELL_EFFECT_NPC
{
   #my $cl = $entity_list->GetClientByID($caster_id);
   #if ($cl)
   #{
   #     $cl->CastSpell(1636, $caster_id);
   #}
}
